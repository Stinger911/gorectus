# Test environment configuration
PORT=8080
GIN_MODE=test
LOG_LEVEL=error

# Test database configuration (won't be used in tests due to mocking)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=test
DB_NAME=gorectus_test
DB_SSLMODE=disable
